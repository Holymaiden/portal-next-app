import Wrapper from "@/common/components/layouts/Wrapper/Wrapper";

const SignUpLayout = ({ children }: { children: React.ReactNode }) => {
  return <Wrapper>{children}</Wrapper>;
};

export default SignUpLayout;
